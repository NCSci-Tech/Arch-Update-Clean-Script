# Arch-Update-Clean-Script
This script automates regular system maintenance tasks for Arch-based distributions. It updates system packages, upgrades AUR packages, removes orphaned dependencies, cleans package caches, and updates system databases.
